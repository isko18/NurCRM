# Установка Redis на Windows - Простое решение

## 🎯 Проблема
Redis не запущен. Ошибка: `Error 10061 connecting to 127.0.0.1:6379`

## ✅ Решение: Docker (Самый простой способ)

### Шаг 1: Установите Docker Desktop (если еще не установлен)

1. Скачайте: https://www.docker.com/products/docker-desktop/
2. Установите и запустите Docker Desktop
3. Дождитесь, пока Docker запустится (иконка в трее)

### Шаг 2: Запустите Redis в Docker

```powershell
docker run -d --name redis-nurcrm -p 6379:6379 redis:7-alpine
```

### Шаг 3: Проверка

```powershell
# Проверка, что контейнер запущен
docker ps | findstr redis

# Проверка подключения
docker exec -it redis-nurcrm redis-cli ping
# Должно вернуть: PONG
```

### Шаг 4: Тест кэша Django

```powershell
python manage.py test_cache
```

---

## 🔄 Автозапуск Redis при старте Windows

Создайте файл `start_redis.ps1`:

```powershell
# start_redis.ps1
docker start redis-nurcrm
```

Добавьте в автозагрузку Windows или запускайте вручную при необходимости.

---

## 🛑 Остановка Redis

```powershell
docker stop redis-nurcrm
```

## 🗑️ Удаление контейнера

```powershell
docker stop redis-nurcrm
docker rm redis-nurcrm
```

---

## 📝 Альтернативные варианты

### Вариант 2: Memurai (Windows-версия Redis)

1. Скачайте: https://www.memurai.com/get-memurai
2. Установите (установится как служба Windows)
3. Готово! Redis будет запускаться автоматически

### Вариант 3: WSL2 (для разработки)

Если хотите использовать WSL2:

```powershell
# Установка WSL2
wsl --install

# После перезагрузки, в WSL:
sudo apt update
sudo apt install redis-server -y
sudo service redis-server start
```

---

## ✅ После установки

После установки любого варианта проверьте:

```powershell
python manage.py test_cache
```

Все тесты должны пройти успешно! ✅

---

*Документ создан: 2025-01-27*

