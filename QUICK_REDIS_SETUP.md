# Быстрая настройка Redis кэширования

## 🚀 Быстрый старт (5 минут)

### Windows 🪟

**Вариант 1: WSL2 (Рекомендуется)**
```powershell
# Установите WSL2 (если еще не установлен)
wsl --install

# Перезагрузите компьютер, затем в WSL:
sudo apt update
sudo apt install redis-server -y
sudo service redis-server start
```

**Вариант 2: Docker**
```powershell
docker run -d --name redis-nurcrm -p 6379:6379 redis:7-alpine
```

**Вариант 3: Memurai**
Скачайте и установите: https://www.memurai.com/get-memurai

📖 **Подробная инструкция:** См. `REDIS_WINDOWS_SETUP.md`

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install redis-server -y
sudo systemctl start redis-server
sudo systemctl enable redis-server
```

### 2. Проверка Redis

```bash
redis-cli ping
# Должно вернуть: PONG
```

### 3. Настройка пароля (рекомендуется)

```bash
sudo nano /etc/redis/redis.conf
```

Найдите и раскомментируйте:
```conf
requirepass ваш_надежный_пароль
```

Перезапустите:
```bash
sudo systemctl restart redis-server
```

### 4. Создание .env файла

Создайте файл `.env` в корне проекта:

```bash
# .env
REDIS_URL=redis://127.0.0.1:6379/1
# Если есть пароль:
# REDIS_PASSWORD=ваш_пароль
# или в URL:
# REDIS_URL=redis://:ваш_пароль@127.0.0.1:6379/1
```

### 5. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 6. Тестирование

```bash
python manage.py test_cache
```

Если все тесты прошли успешно - кэширование настроено! ✅

---

## 📝 Полная инструкция

Смотрите `REDIS_SETUP_GUIDE.md` для детальной настройки.

---

## ⚠️ Важно для продакшена

1. **Обязательно установите пароль Redis**
2. **Настройте firewall** (не открывайте порт 6379 наружу)
3. **Используйте переменные окружения** для всех настроек
4. **Настройте мониторинг** Redis
5. **Настройте резервное копирование**

---

## 🔧 Полезные команды

```bash
# Статус Redis
sudo systemctl status redis-server

# Перезапуск
sudo systemctl restart redis-server

# Логи
sudo tail -f /var/log/redis/redis-server.log

# Подключение к Redis
redis-cli
# или с паролем:
redis-cli -a ваш_пароль

# Очистка кэша Django
python manage.py shell -c "from django.core.cache import cache; cache.clear()"
```

