# Cash Shift ↔ CashFlow (Market / Магазин) — Детальный технический разбор

Документ содержит исчерпывающее описание механик работы кассовых смен () и движения средств () в секторе Market / Магазин, точные формулы всех агрегатов, правила привязки, варианты контракта API для фронтенда и детализированный разбор кейса с изъятием 150 сом.

---

## 1. Краткий вердикт: Почему 150 сом не попали в  смены

1. **Причина на стороне вызова**: Фронтенд при создании движения кассы () не передал в теле запроса параметр .
2. **Правило бэкенда**: В сериализаторе  зафиксирована логика: *если  не передан в запросе, бэкенд НЕ ищет смену автоматически и сохраняет ордер с * (как общее движение по кассе вне смены).
3. **Результат в смене**: Метод  считает расчётные приходы/расходы смены строго по условию:
   
   Поскольку у ордера на 150 сом , он не попал в выборку этой смены,  остался равен , а  не уменьшился на 150 сом.
4. **Дополнительная причина (если применимо)**: Если статус ордера был  (в ожидании), он не входит в агрегаты смены до перевода в статус .

---

## 2. Пути к файлам в кодовой базе

- **Модели смен и движений кассы (, , )**: 
- **Сериализаторы смен и ордеров (, , )**: 
- **Представления / Endpoints (, , )**: 
- **Сервисы автозакрытия и регламентных работ**: 
- **Связка с POS-продажами и чекаутом**: 
- **Аналитический блок смен**: 

---

## 3. Таблица и контракт полей смены ()

| Поле | Тип | Хранение (Stored / Computed) | Когда пересчитывается | Точная формула / Queryset |
|---|---|---|---|---|
|  | Decimal | Stored | При  | Передаётся фронтендом при открытии смены (по умолчанию ). |
|  | Decimal | Stored | При  | Передаётся фронтендом при закрытии смены (фактическая наличность в кассе). |
|  | Integer | Live (OPEN) / Stored (CLOSED) | На каждый  / при  |  |
|  | Decimal | Live (OPEN) / Stored (CLOSED) | На каждый  / при  |  |
|  | Decimal | Live (OPEN) / Stored (CLOSED) | На каждый  / при  |  |
|  | Decimal | Live (OPEN) / Stored (CLOSED) | На каждый  / при  |  |
|  | Decimal | Live (OPEN) / Stored (CLOSED) | На каждый  / при  |  |
|  | Decimal | Live (OPEN) / Stored (CLOSED) | На каждый  / при  |  |
|  | Decimal | Computed Property | На каждый вызов свойства |  |
|  | Decimal | Computed Property | На каждый вызов свойства |  (для  смен = ) |

---

## 4. Правила привязки CashFlow → Shift

1. **Если  передан фронтендом ():**
   - Бэкенд проверяет: , , и  (если не owner).
   - Ордер привязывается к смене ().
   - Если , он автоматически включается в  или  этой смены.

2. **Если  НЕ передан (или передан ):**
   - Ордер сохраняется с .
   - Ордер учитывается в общем балансе кассы , но **НЕ входит ни в одну смену** (не меняет  смены).

3. **Если :**
   - Попадает в базу данных, но **НЕ входит** в  /  до перевода в статус .
   - При одобрении ( или из админки) ордер переводится в  и сразу начинает учитываться в  смены.

---

## 5. Рекомендации и Контракт API для Фронтенда

### 1. Создание прихода / расхода из кассы ()
При создании оперативного изъятия кассиром фронтенд **обязан передавать **:


### 2. Откуда брать ?
При открытии смены () или при получении активной смены () фронтенд получает объект смены. Значение uid=1000(fondante) gid=1000(fondante) группы=1000(fondante),65534(nobody) из этого объекта передаётся в поле .

### 3. Закрытие смены ()
Фронтенд на странице закрытия смены полностью доверяет полям , ,  из ответа . Бэкенд не блокирует закрытие при .

---

## 6. Таблица краевых кейсов (Edge Cases)

| Сценарий |  /  |  |  | Примечание |
|---|---|---|---|---|
| **Обычная продажа налом (1000 сом)** | Не меняется | +1000.00 | +1000.00 | Через  |
| **Продажа безналом (2000 сом)** | Не меняется | 0.00 | Не меняется | Попадает в  (+2000.00) |
| **Смешанная оплата (500 нал + 500 карта)** | Не меняется | +500.00 | +500.00 | Нал -> , Карта ->  |
| **Продажа в долг / отсрочку ()** | Не меняется | 0.00 | Не меняется | Не попадает в наличную кассу |
| **Изъятие налом 150 сом ( передан, )** |  +150 | Не меняется | -150.00 | Корректно уменьшает наличность |
| **Изъятие налом 150 сом ( НЕ передан)** | 0.00 | Не меняется | Не меняется | Баговый кейс: привязано к кассе, но не к смене |
| **Изъятие налом 150 сом ()** | 0.00 | Не меняется | Не меняется | Попадёт в смену только после  |
| **Возврат товара налом (500 сом)** | Не меняется | -500.00 | -500.00 |  пропорционально снижается |

---

## 7. Чеклист диагностики бага «Изъяли 150 сом, на закрытии 0»

Если в смене , но изъятие было произведено:

1. Выполнить SQL-запрос в базе данных:
   sql: Error: No DBURL given

Usage:
sql [-hnr] [--table-size] [--db-size] [-p pass-through] [-s string] dburl [sqlcommand]
sql [-hnr] [--table-size] [--db-size] [-p pass-through] [-s string] dburl < sql_command_file

See 'man sql' for the options
2. Проверить значение :
   - Если  -> Фронтенд не передал  в запросе .
3. Проверить значение :
   - Если  -> Заявка не утверждена администратором.

---

## 8. Инициативы по улучшению бэкенда

Если бизнес-требование гласит, что кассир в принципе не должен иметь возможности создать расход мимо смены:
- В  /  бэкенда можно добавить авто-подстановку активной открытой смены кассира по данной кассе (), если параметр  не был явно передан в теле запроса.
